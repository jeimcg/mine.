import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#fbdfb2",
    },
    secondary: {
      main: "#f48fb1",
    },
  },
});

export default theme;
